@model MVC.Models.PaisesModel

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<h2>Paises Index</h2>
<p>
    @Html.ActionLink("Crear nueva", "Crear", "Paises", new { @class = "btn btn-primary" })
</p>
<table class="table" id="dem">

    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Pais.Nombre)
            </th>
            <th>
                Codigo de Pais

                    
            </th>
            <th>

            </th>

            <th>




            </th>
            <th></th>
        </tr>

    </thead>

    <tbody>

        @foreach (var item in Model.Paises)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nombre)
                </td>
                <td>

                    @Html.DisplayFor(modelItem => item.Id)

                </td>
                <td>
                </td>

                <td>
                </td>

                <td>
                    <a class="btn btn-primary btn-xs" title="Editar" href="@Url.Action("Editar", "Paises", new { id = item.Id })"><i class="glyphicon glyphicon-edit"></i></a>

                    <a class="btn btn-success btn-xs" title="Detalles" href="@Url.Action("GetPais", "Paises", new { id = item.Id })"><i class="glyphicon glyphicon-file"></i></a>

                    <a class="btn btn-danger btn-xs" title="Eliminar" href="@Url.Action("Eliminar", "Paises", new { id = item.Id })"><i class=" glyphicon glyphicon-trash"></i></a>
                </td>
            </tr>
        }

    </tbody>


</table>


@section scripts
    {



    <script>

        $(document).ready(function () {
            $('#dem').DataTable();
        });

    </script>

}