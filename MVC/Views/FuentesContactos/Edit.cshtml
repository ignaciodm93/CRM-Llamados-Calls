@model MVC.Models.FuentesContactosModel

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm("Edit", "FuentesContactos", FormMethod.Post))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>FuentesContactosModel</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(m => m.FuentesContactos.Id)



        <div class="form-group">
            <h5 class="control-label col-lg-3">Fuente</h5>
            @Html.TextBoxFor(m => m.FuentesContactos.Nombre, htmlAttributes: new { @class = "form-control" })



            @Html.ValidationMessageFor(model => model.FuentesContactos.Nombre, "", new { @class = "text-danger" })
        </div>
    







    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Save" class="btn btn-default" />
        </div>
    </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
